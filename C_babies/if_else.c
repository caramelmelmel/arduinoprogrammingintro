#include <stdio.h>

//again we write the following function 
int main(){
    int a = 5;
    if (a==1)
        printf("Nice try dude!");
    else if (a==2)
        printf("Sup");
    else{
        return a;}
}